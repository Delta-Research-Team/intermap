name: intermap
channels:
  - defaults
  - conda-forge
dependencies:
  # not necessary for the package. Remove when published.
  - prolif
  - matplotlib
  - ipython
  - pytest
  - pytest-cov

  - python>=3.6
  - pip
  - numpy==1.23.1
  - pandas
  - tqdm

  - numba
  - mdanalysis
  - rdkit
  - bitarray == 3.0.0
  - mdtraj == 1.9.4

  - pip:
      - -e .
      - numpy_indexed
      - numba_kdtree
      - pytest-randomly
      - openpyxl
      - git+https://github.com/rglez/rgpack.git@0.0.3
